<!--
  Fill in <PROJECT_NAME>, the one-liner, and the Vision below, then
  replace the Quick start commands with the real ones for this stack
  (delete steps that don't apply). This file is *what the project is* —
  vision, status, how to run it. HOW we work lives in AGENTS.md; the
  *why* behind locked choices lives in docs/decisions.md.
-->

# \<PROJECT_NAME\>

\<one-line description — what this is and who it's for\>

## Vision

<!-- 2-4 sentences: what this project is, the problem it solves, and
     why it's worth building. The north star, not the backlog. -->

\<What is this, and why does it exist?\>

## Status

<!-- One line the reader can trust: pre-first-run / building the first
     vertical slice / usable / stable. Update it as reality changes. -->

\<current state — e.g. "early: first vertical slice in progress"\>

## Quick start

<!-- The real build/run/test commands for this stack. An operator
     should be able to copy-paste these and see it work. -->

```sh
<clone / install deps>     # e.g. git clone … && cd … && <install>
<build or run>             # e.g. make dev  /  npm run dev  /  cargo run
<test>                     # e.g. make verify  /  npm test
```

## Layout

Where things live, and where to look next:

- **[AGENTS.md](AGENTS.md)** — *how we work* here: conventions, the
  commit/journal/verify loop, when to stop and ask. Read this first if
  you're contributing (human or agent).
- **[docs/](docs/)** — what the project *is now*: architecture, design,
  living reference. Edited in place.
- **[docs/decisions.md](docs/decisions.md)** — the *why*: an append-only
  ADR log of locked decisions and the alternatives they beat.
- **[project/journal/](project/journal/)** — the *history*: an
  append-only, per-session log of how it got here.

<!-- Add a short map of the source tree here once it exists, e.g.
     `src/` — core logic;  `adapters/` — I/O.  Keep it to a few lines. -->
